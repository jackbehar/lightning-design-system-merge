import * as React from "react";
import Button from "../Button";

export default (
  <Button
    uxpId="button"
    disabled={false}
    iconCategory="utility"
    iconName="download"
    label="Buttons">
  </Button>
);
