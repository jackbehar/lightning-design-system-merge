import * as React from "react";
import Icon from "../Icon";

export default (
  <Icon
    uxpId="button"
    assistiveText={{ label: 'Account' }}
    category="standard"
    name="account"
    size="small"
    />
);
